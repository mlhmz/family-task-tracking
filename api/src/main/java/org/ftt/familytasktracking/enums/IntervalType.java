package org.ftt.familytasktracking.enums;

/**
 * The interval type defines, as the name suggests, the interval of tasks combined with an integer.
 */
public enum IntervalType {
    HOURS,
    DAYS,
    WEEKS,
    MONTHS,
    YEARS,
}
